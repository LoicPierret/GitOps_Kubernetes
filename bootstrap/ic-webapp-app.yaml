apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ic-webapp
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/LoicPierret/GitOps_Kubernetes.git
    targetRevision: main
    path: apps/ic-webapp
  destination:
    server: https://kubernetes.default.svc
    namespace: ic-webapp
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
